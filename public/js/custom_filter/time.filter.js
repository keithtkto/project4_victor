(function() {
  'use strict';

  angular.module('customFilter', [])

    .filter('timeCompare', function() {

      return function(timeArray) {
        var newTimeCardArray;

        newTimeCardArray = timeCardArray.filter(function(timecard){

        })

        return newTimeCardArray;
      };

    })


})();
